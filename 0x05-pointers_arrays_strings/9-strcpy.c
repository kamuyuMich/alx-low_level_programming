#include "main.h"

/**
 * _strcpy - Copies a string
 * @dest: Destination
 * @src: Source
 * Return: The pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	int i;

	for (i = 0; scr[i] != '\0'; i++)
	{
		dest[i] = scr[i];
	}
	dest[i++] = '\0';
	return (dest);
}
